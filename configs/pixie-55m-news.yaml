import: default.yaml

override:
  train.pretrain.dataset:
    name: vblagoje/cc_news
    ratio: 0.5
  train.pretrain.context_length: 512
  train.pretrain.batch_size: 16

train:
  sft: null
  dpo: null
